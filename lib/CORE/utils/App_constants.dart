class AppConstants {
  static const String baseUrl = 'https://fapi.binance.com/fapi/v1/exchangeInfo';
  static const Duration defaultTimeout = Duration(seconds: 30);
  static const double defaultPadding = 16.0;
  static const int pageSize = 20;
}